"""PaperWhirl's web layer — FastAPI backend + bundled React frontend.

The `server` submodule defines the FastAPI app and the `main()`
function that the `paperwhirl` console script calls. The static
React build lives under `frontend_dist/` as package data.
"""
